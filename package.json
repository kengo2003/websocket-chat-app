{
  "name": "root",
  "scripts": {
    "install:all": "cd client && bun install && cd ../server && uv sync",
    "dev": "bun run dev:client & bun run dev:server",
    "dev:client": "cd client && bun run dev",
    "dev:server": "cd server && uv run uvicorn main:app --reload"
  }
}
